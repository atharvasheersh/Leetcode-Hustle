class Solution:
    def isPalindrome(self, x: int) -> bool:
        str_x = str(x) 
        l_rev = str_x[::-1]  
        if l_rev == str_x:  
            return True
        else:
            return False
